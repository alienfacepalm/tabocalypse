packages:
  - "apps/*"
  - "packages/*"

allowBuilds:
  esbuild: true
  sharp: true
  spawn-sync: true

# pnpm 11+ reads overrides here only; package.json `pnpm.overrides` is ignored.
overrides:
  "@vitejs/plugin-react": "4.4.1"
