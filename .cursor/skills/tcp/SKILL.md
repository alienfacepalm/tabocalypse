---
name: tcp
description: >-
  Alias for check-commit-push — test, commit, and push after pnpm check passes.
  Use when the user says "tcp", "test commit and push", or "test, commit and push".
disable-model-invocation: true
---

# tcp — test, commit, and push

**Same workflow as [check-commit-push](../check-commit-push/SKILL.md).** Read and follow that skill end-to-end.

Trigger phrases **tcp** and **test, commit, and push** map here; **check, test, commit, and push** maps to check-commit-push directly.
