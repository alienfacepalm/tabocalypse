#!/bin/sh
set -e

echo "Running pre-push checks..."

# Skip if no staged files (pushing untracked branches)
if ! git diff --cached --quiet 2>/dev/null; then
  echo "Staged files detected, running full check..."
  pnpm check
else
  echo "No staged files — skipping full check (untracked branch push)"
fi
