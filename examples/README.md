# Examples

- **Humor pack** — [`pack-starter.json`](pack-starter.json) (`pack.json` format for ZIP import).
- **Declarative plugin** — moved to [`packages/example-plugin`](../packages/example-plugin) (`@tabocalypse/example-plugin`).
