# Examples

- **Humor pack** — [`pack-starter.json`](pack-starter.json) (`pack.json` format). Import it under **Settings › Import pack** either as the plain `.json` file or as a `pack.json` inside a `.zip`.
- **Declarative plugin** — moved to [`packages/example-plugin`](../packages/example-plugin) (`@tabocalypse/example-plugin`).
