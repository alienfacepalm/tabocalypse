# Design reference

The canonical design spec lives at the **repository root**: [`DESIGN.md`](../DESIGN.md).

This `design/` folder holds **HTML example mocks** for UX reference only; do not duplicate the spec here.
