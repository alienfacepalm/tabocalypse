/** @see https://prettier.io/docs/en/options.html — explicit Prettier defaults (matches existing Tabocalypse TS/TSX style). */
const config = {
  semi: true,
  singleQuote: false,
  trailingComma: "all",
  printWidth: 100,
  tabWidth: 2,
  useTabs: false,
  arrowParens: "always",
  endOfLine: "auto",
};

export default config;
